import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: 300,
    height: 40,
    margin: 10,
    borderWidth: 1,
    borderRadius: 20,
    borderColor: 'gray',
    paddingHorizontal: 20,
  },
  textMessage: {
    marginVertical: 20,
  },
});
